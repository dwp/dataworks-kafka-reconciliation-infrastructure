groups:
  - name: master
    jobs:
      - development
      - qa
  - name: pull-request
    jobs:
      - dataworks-kafka-reconciliation-infrastructure-pr
  - name: update-pipeline
    jobs:
      - update-pipeline
