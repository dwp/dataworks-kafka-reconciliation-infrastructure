groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
      - preprod
      - production

  - name: pull-request
    jobs:
      - dataworks-kafka-reconciliation-infrastructure-pr
  - name: update-pipeline
    jobs:
      - update-pipeline
